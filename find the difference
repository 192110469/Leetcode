class Solution {
    public char findTheDifference(String s, String t) {
        int value=0;
        for(char c:t.toCharArray()){
            value+=c;
        }
        for(char c:s.toCharArray()){
            value-=c;
        }
        return (char)value;
    }
}
